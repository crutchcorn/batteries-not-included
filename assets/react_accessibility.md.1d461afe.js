import{_ as e,o as t,c as o,N as a}from"./chunks/framework.c6f03c5f.js";const m=JSON.parse('{"title":"Accessibility","description":"","frontmatter":{},"headers":[],"relativePath":"react/accessibility.md","lastUpdated":1694038825000}'),i={name:"react/accessibility.md"},n=a('<h1 id="accessibility" tabindex="-1">Accessibility <a class="header-anchor" href="#accessibility" aria-label="Permalink to &quot;Accessibility&quot;">​</a></h1><p>This set of utilities is meant to help developers create accessible components without making assumptions about how it&#39;s UI will display</p><h2 id="id-generation" tabindex="-1">ID Generation <a class="header-anchor" href="#id-generation" aria-label="Permalink to &quot;ID Generation&quot;">​</a></h2><table><thead><tr><th>File</th><th>Function</th></tr></thead><tbody><tr><td><code>genNewId</code></td><td><code>genId</code></td></tr></tbody></table><p>This function has no parameters, but will return a number that&#39;s unique to that instance. We use it to generate new IDs that can be used by an <code>id</code> attribute</p><h2 id="keyboard-detection" tabindex="-1">Keyboard Detection <a class="header-anchor" href="#keyboard-detection" aria-label="Permalink to &quot;Keyboard Detection&quot;">​</a></h2><p>There may be an instance where you want to check if the user last used a keyboard on an element in order to render different UI (or highlight items when the user is using a keyboard), this hook may be of help</p><table><thead><tr><th>File</th><th>Function</th></tr></thead><tbody><tr><td><code>useUsedKeyboardLast</code></td><td><code>useUsedKeyboardLast</code></td></tr></tbody></table><p>This hook takes two parameters:</p><ol><li><code>elRef</code>: A reference to an element used to attach the event listeners to</li><li><code>options</code>: An optional object of options for the hook</li></ol><h3 id="options-object" tabindex="-1">Options Object <a class="header-anchor" href="#options-object" aria-label="Permalink to &quot;Options Object&quot;">​</a></h3><ul><li><code>options.enable</code>: A boolean of if it should be enabled or not. If disabled, the <code>usedKeyboardLast</code> value will remain the same</li></ul><h3 id="returns" tabindex="-1">Returns <a class="header-anchor" href="#returns" aria-label="Permalink to &quot;Returns&quot;">​</a></h3><p>This hook returns an object with two keys:</p><ul><li><code>usedKeyboardLast</code>: A boolean of if the user had last used a keyboard</li><li><code>resetLastUsedKeyboard</code>: A function that takes no parameters and sets <code>usedKeyboardLast</code> to false</li></ul><h2 id="keyboard-list-navigation" tabindex="-1">Keyboard List Navigation <a class="header-anchor" href="#keyboard-list-navigation" aria-label="Permalink to &quot;Keyboard List Navigation&quot;">​</a></h2><p>There are various UI elements that may want to use keyboard arrow key navigation on. This hook will assist you in handling this, although it will not handle the &quot;selection&quot; element of a UI element that might expect that.</p><p>By default, it handles up arrow, down arrow, end and home button navigation. The up and down arrow moves by one and the home and end go to the first and last index item</p><table><thead><tr><th>File</th><th>Function</th></tr></thead><tbody><tr><td><code>useKeyboardListNavigation</code></td><td><code>useKeyboardListNavigation</code></td></tr></tbody></table><p>This hook takes two parameters:</p><ol><li><code>parentRef</code>: A reference to an element used to attach the event listeners to</li><li><code>options</code>: An optional object of options for the hook</li></ol><h3 id="options-object-1" tabindex="-1">Options Object <a class="header-anchor" href="#options-object-1" aria-label="Permalink to &quot;Options Object&quot;">​</a></h3><ul><li><code>options.maxLength</code>: The maximum number that should be allowed. By default, <ul><li>If this is not defined, home and end buttons will not work</li></ul></li><li><code>options.enable</code>: A boolean of if it should be enabled or not</li><li><code>options.runOnIndexChange</code>: A callback that should be ran whenever the selected index is changed</li><li><code>options.wrapOnOverflow</code>: A boolean. When false, we force numbers within set min/max constraints, otherwise if true, we allow negative numbers to set the focused index to the minimum and numbers larger than the maximum to set the focused index to the minimum value</li></ul><h4 id="run-on-index-change-callback" tabindex="-1">Run On Index Change Callback <a class="header-anchor" href="#run-on-index-change-callback" aria-label="Permalink to &quot;Run On Index Change Callback&quot;">​</a></h4><p>The <code>runOnIndexChange</code> callback allows a developer the ability to add additional key listeners to the existing keypress listener, or run other custom code when the user changes the index of the currently selected item</p><ul><li><code>event</code>: The keyboard event that&#39;s used on the element, useful for extending functionality of the keyboard listener for more keypress checks <ul><li>Be aware that if you use <code>selectIndex</code>, this may be <code>undefined</code> if you do not manually pass the keyboard event</li></ul></li><li><code>focusedIndex</code>: The previous index that was selected before user interaction</li><li><code>newIndex</code>: The new index that&#39;s now being selected by user interaction <ul><li>Be aware that this may also be <code>undefined</code> if the user selects a key that is not being bound to any specific index value</li></ul></li></ul><p>It&#39;s useful to have both the old and new index for scenarios like allowing a user to have a selection that&#39;s toggled when the user holds shift and selects two indexes</p><h3 id="returns-1" tabindex="-1">Returns <a class="header-anchor" href="#returns-1" aria-label="Permalink to &quot;Returns&quot;">​</a></h3><p>The <code>useKeyboardListNavigation</code> hook returns an object with two keys:</p><ul><li><code>focusedIndex</code>: The index of the currently focused item</li><li><code>selectIndex</code>: A function that takes two parameters: <ol><li><code>index</code>: The new index that you&#39;d like to have selected</li><li><code>event</code>: The event that was used to trigger the selection. This will get passed to <code>runOnIndexChange</code>, so it&#39;s suggested to pass it when the user makes keyboard events at least</li></ol></li></ul><h2 id="selectable-array" tabindex="-1">Selectable Array <a class="header-anchor" href="#selectable-array" aria-label="Permalink to &quot;Selectable Array&quot;">​</a></h2><p>In many UI elements, there are times where having a selectable array with unique elements and a state of if an item is selected or not can be greatly useful. This hook assists in scenarios of the sort</p><table><thead><tr><th>File</th><th>Function</th></tr></thead><tbody><tr><td><code>useSelectableArray</code></td><td><code>useSelectableArray</code></td></tr></tbody></table><p>This base hook takes two parameters:</p><ol><li><code>valArr</code>: The original value array that you want to make selectable</li><li><code>runAfterSelectChange</code>: An optional function that can be ran after selection changes are made <ul><li>This is present as the <code>ref.current</code> value changes may not trigger a detection change, so this allows you to do so manually</li></ul></li></ol><h3 id="returns-2" tabindex="-1">Returns <a class="header-anchor" href="#returns-2" aria-label="Permalink to &quot;Returns&quot;">​</a></h3><p>This hook returns an object with three keys:</p><ul><li><code>internalArr</code>: An array the same length as <code>valArr</code>, but with more metadata on each item in the array <ul><li>Each item of the array will have the following values: <ul><li><code>id</code>: A unique ID that can be safely placed in the DOM</li><li><code>val</code>: The original value in the <code>valArr</code> array</li><li><code>index</code>: The original index in the <code>valArr</code> array</li><li><code>selected</code>: If this item is selected in the data</li></ul></li></ul></li><li><code>selectAll</code>: A function with no parameters that will mark all items in the array with <code>selected: true</code></li><li><code>markAsSelected</code>: A function that takes two parameters - a from index and a to index. It will then select all the items from one value to another <ul><li>If the <code>from</code> index is lower than the <code>to</code> index, it will simply swap the two numbers and mark the items in between as <code>selected: true</code>0</li></ul></li></ul><h2 id="popover" tabindex="-1">Popover <a class="header-anchor" href="#popover" aria-label="Permalink to &quot;Popover&quot;">​</a></h2><p>This hook is meant to provide a utility that can be used to compose functionality for a popover component. A popover component is one that has a trigger button and a popup after the button is pressed. This hook handles the <code>expanded</code> property as well as the props to be added to the trigger button</p><table><thead><tr><th>File</th><th>Function</th></tr></thead><tbody><tr><td><code>usePopover</code></td><td><code>usePopover</code></td></tr></tbody></table><p>This hook takes three parameters:</p><ol><li><code>parentRef</code>: The div that contains the popoverArea and the trigger button</li><li><code>popoverAreaRef</code>: The div that will be used as the popover area to focus on when the popover is opened</li><li><code>options</code>: An optional object of options for the hook</li></ol><h3 id="options-object-2" tabindex="-1">Options Object <a class="header-anchor" href="#options-object-2" aria-label="Permalink to &quot;Options Object&quot;">​</a></h3><ul><li><code>options.onBtnClick</code></li><li><code>options.onBtnKeyDown</code></li></ul><p>Because the return object expects you to pass the <code>buttonProps</code> <code>onClick</code> and <code>onKeyDown</code> to the button used to trigger a popover, passing these functions will run after the <code>usePopover</code> hook&#39;s implementation of each of these functions, allowing you to configure the behavior of them manually without ejecting from the object property spread.</p><h3 id="returns-3" tabindex="-1">Returns <a class="header-anchor" href="#returns-3" aria-label="Permalink to &quot;Returns&quot;">​</a></h3><p>The <code>usePopover</code> hook returns an object with three keys:</p><ul><li><code>buttonProps</code>: The set of props that should be expanded on into a button. IE: <code>&lt;button {...buttonProps}/&gt;</code><ul><li>The two included props in this object are <code>onClick</code> and <code>onKeyDown</code>. Your own implementations of these will be overwritten if you follow the above code</li></ul></li><li><code>expanded</code>: A boolean of if the popover is expanded or not</li><li><code>setExpanded</code>: A function that takes a single boolean parameter and sets the value of <code>expanded</code></li></ul><h2 id="popover-combobox" tabindex="-1">Popover Combobox <a class="header-anchor" href="#popover-combobox" aria-label="Permalink to &quot;Popover Combobox&quot;">​</a></h2><p>A combobox being a way of selecting multiple items in a list at once, a popover combobox is just what it sounds like: A popup multi-select. This hook is meant to serve as a general utility for multi-select, pop-over combo-box. It composes the ability to have a popover, keyboard navigation multi-selects, and tests to see if the user had last used a keyboard</p><table><thead><tr><th>File</th><th>Function</th></tr></thead><tbody><tr><td><code>usePopoverCombobox</code></td><td><code>usePopoverCombobox</code></td></tr></tbody></table><p>This hook take a single parameter:</p><ol><li>An array of values that can be selected that should be used to display the combobox</li></ol><h3 id="returns-4" tabindex="-1">Returns <a class="header-anchor" href="#returns-4" aria-label="Permalink to &quot;Returns&quot;">​</a></h3><p>The <code>usePopoverCombobox</code> hook returns an object with three keys:</p><ul><li><code>selected</code></li><li><code>active</code></li><li><code>comboBoxListRef</code></li><li><code>parentRef</code></li><li><code>values</code></li><li><code>selectIndex</code></li><li><code>expanded</code></li><li><code>setExpanded</code></li><li><code>usedKeyboardLast</code></li><li><code>buttonProps</code></li></ul>',57),d=[n];function l(s,r,c,h,u,b){return t(),o("div",null,d)}const f=e(i,[["render",l]]);export{m as __pageData,f as default};
